const saoIguais = num1 === num2;
const soma = num1 + num2;


// Return if
    if(saoIguais) {
        return "São iguais";
    }

    return "Não são iguais";

// Return if ternário
    return saoIguais ? "São iguais" : "Nào são iguais"